# Security

Please do not include access tokens, unredacted `hooks.json`, `notify.log`, or Codex rollout files in public issues.

The watcher reads local Codex rollout files only to identify status changes. It does not modify those files, make network requests, or send telemetry.

For a security concern, open a GitHub security advisory instead of a public issue when that feature is available for the repository.
